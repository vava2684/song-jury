---
name: song-jury
description: 歌曲評審團(九柱制,滿分 100)。曲側八柱由程式跑:聲學量測(響度/動態/頻譜/立體聲)、Demucs 六軌編曲層次、Viterbi 和弦辨識、演唱量測、SongEval 音樂人五維、Meta Audiobox 四軸、Gemini 六維證據型曲評、SingMOS 演唱聽感、MuQ 真實距離;詞柱由 AI 依中/英/日/韓四把語言尺評(八家五輪對抗定版)。輸入吃 SUNO 連結/YouTube 連結/本機音檔;三模式:單評、多首 PK(僅限同語種)、重複抽卡比較。產出成績單表格+md+PDF+長圖報告。觸發時機:使用者說「評歌」「評這首」「歌曲評分」「評審團」「跑評分」「PK 這幾首」「評詞」「幫我看這首歌」「重複抽卡比較」「比 take」「這幾個抽卡哪個好」等。
---

# song-jury — 歌曲評審團(九柱制)

> **自足 skill**:整個 repo(工具 + venv + 評詞標準.md + rubrics)就在本 skill 資料夾內,
> `$T` = **本 SKILL.md 所在的資料夾**(自我定位,不寫死路徑)。
> **安裝**:把 repo clone 進 `~/.claude/skills/song-jury/`,在該夾雙擊 `一鍵安裝.bat`
> 或跑 `./install.ps1`(Windows)/ `bash install.sh`(Linux/Mac)建四個 venv、抓模型。
> 事後想確認哪幾柱能用:`./install.ps1 -CheckOnly` / `bash install.sh --check-only`。
> **評詞規則的唯一真理來源:`$T\評詞標準.md` + `$T\rubrics\`——每次評詞前必讀,本檔不重複其內容。**
> **報告格式的唯一真理來源也是 `評詞標準.md`**(兩層結構、解讀欄零留空、治理術語不進報告)。

## 開場協議(第一句必做)

1. 請使用者選模式:**單一模式**(只評這首,禁比較禁排名)、**PK 模式**(多首並排對決,⛔**僅限同語種** —— 四把尺維度數與軸不可共量)、或 **重複抽卡比較**(同一份詞+prompt 的多個 SUNO take 並排;詞共用評一次,只比曲側)。⛔ 後兩種**一律用 `比較.py`** 產生排名與落差,不可以自己在對話裡算 —— 那樣同一批資料在不同對話會得出不同冠軍。
2. 告知輸入方式:**SUNO 連結**(自動抓歌+歌詞+正式歌名)、**YouTube 連結**(抓歌,歌詞要另給)或 **直接給檔**(本機路徑 + 歌詞)
3. 使用者沒表態模式前不開跑

## 執行流程

```powershell
$T = "<本 skill 資料夾絕對路徑>"   # 自足:設成本 SKILL.md 所在目錄
$env:PYTHONUTF8 = "1"

# ① 曲側八柱一次跑完(產 _評審團.json)
& "$T\.venv\Scripts\python.exe" "$T\評審團.py" <歌檔或 SUNO/YouTube 連結>
$juryRc = $LASTEXITCODE   # ⛔ 立刻保存 —— 退出碼是完整性契約,不看等於裝瞎

# ⛔ 退出碼契約(必判,不可跳過):
#   0 = 完整評測          → 正常繼續 ②③,做完整交付
#   2 = 報告已完整發布但【缺柱】 → JSON 照讀、報告照做,但整份交付必須
#       在最上方標「⛔ 不完整評測(缺柱),不可排行、不可與他歌互比」;
#       ⛔ 不可把 2 當一般錯誤丟掉報告 —— 那是一次昂貴評測的完整產物
#   其他 = 執行失敗        → 停止後續報告流程,把錯誤原文給使用者
# ⚠️ 退出碼之外還要再讀 JSON 的 pillar_totals.完整評測 做二次確認
#   (兩者理應一致;不一致=產出有問題,以 JSON 為準並向使用者說明)

# ② 歌詞情感弧線(段落用【】標記;產 _情感弧線.png)
& "$T\.venv\Scripts\python.exe" "$T\情感弧線.py" <歌詞.txt>

# ③ PK / 抽卡比較(⛔ 規則寫死在程式裡,不要自己發明公式或權重)
#   PK   :同語言、同 scoring_contract、每份都要完整九柱,否則它會 exit 2 拒絕比較
#   抽卡 :同一份詞+prompt 的多個 take;比的是曲側**八柱全部**,
#          best_take = 曲側合成最高者(不是含糊的「綜合分」)
#   ⛔ 同一份報告複製改名/換容器都不能當兩票:報告帶 evaluation_id +
#      source_file_sha256 + source_audio_pcm_sha256(解碼後,保留原始取樣率
#      與聲道結構,另附 source_audio_pcm_contract 版本),比較器以
#      code=duplicate_source 拒絕;source_identity.level 標證據強度
#      (decoded-audio > exact-file > mixed > weak)。
#      ⚠️ 擋不到 lossy 重壓,也不把「重新取樣/改聲道」的版本當同源。
& "$T\.venv\Scripts\python.exe" "$T\比較.py" pk    --lang zh 甲_評審團.json 乙_評審團.json
& "$T\.venv\Scripts\python.exe" "$T\比較.py" takes --group A t1_評審團.json t2_評審團.json

# ④ 報告 md 寫好後 → PDF → 長圖
& "$T\.venv\Scripts\python.exe" "$T\報告轉PDF.py" <報告.md>
& "$T\.venv\Scripts\python.exe" "$T\轉PNG.py" <報告.pdf>
```

⛔ **一律用 `.venv` 裡的 python**,不要打裸 `python`(相依都在 venv 裡)。

## 九柱制(滿分 100)

```
總分 = 25.3%×詞柱 + 74.7%×曲側八柱加權
```

| 柱 | 權重 | 誰在評 |
|---|:--:|---|
| ✍️ 詞 | 25.3% | **AI 依四把語言尺評**(見下節) |
| 🎤 人聲演唱 | 15.2% | 演唱量測(`.venv`)+ SingMOS(`.venv-audition`)+ Gemini M5 + SongEval |
| 🎼 和聲 | 13.6% | 和弦辨識(chroma + Viterbi,吃 Demucs 分軌) |
| 🏗 結構與編曲 | 12.6% | Demucs 六軌編曲層次 + Gemini M1/M4 + SongEval |
| 🔊 聲學製作 | 12.1% | EBU R128 量測(`.venv`)+ SongEval + Audiobox PQ |
| 🎵 旋律與記憶 | 6.1% | Gemini M2 + SongEval 記憶點 |
| 🌍 真實性與風格 | 6.1% | MuQ 馬氏距離(`.venv-audition`)+ Gemini M6 |
| 🧭 整體音樂性 | 5.1% | Gemini 總分 + SongEval 音樂性 |
| 🥁 律動 | 4.0% | Gemini M3(唯一來源 —— 沒金鑰這柱整根缺) |

⚠️ 九個權重加起來是 **100.1**(合議庭各柱四捨五入的結果),**不影響分數**:
曲側合成是「除以在場柱的權重和」自我歸一化,總分兩係數 0.253/0.747 正好合 1。
⛔ 不可以為了湊 100 私自改任何一格 —— 改一格要單格重開辯論。

**四個環境的分工**(`評審團.py` 自動處理,不必手動逐個跑):

| 環境 | 跑什麼 | 缺了會怎樣 |
|---|---|---|
| `.venv` | 聲學/演唱量測、Gemini 曲評、情感弧線、報告 | 幾乎全部 |
| `.venv-demucs` | Demucs 六軌分離(`分軌快取.py` / `伴奏混音.py`) | **和聲柱 + 結構編曲柱 + 人聲柱大半細項** |
| `.venv-ml` | SongEval 五維 + Audiobox 四軸 | 五個模型聽感細項 |
| `.venv-audition` | SingMOS + MuQ + SONICS | SingMOS、真實距離 |

⭐ **編曲層次排第一**是因為它順便產出人聲軌給演唱量測,和聲分析也共用同一份 `_stems` 快取
→ **Demucs 全程只跑一次**。
⭐ **每一關都是「可選」**:任何一個失敗只會列在成績單末尾的「本次未完全跑到的項目」,
缺的細項會在柱內重新歸一化並在報告標註,**不會中斷產出**。

## 詞柱 —— 依四把語言尺

**AI 是「照尺執行」的人,不是分數的來源。** 四把尺是八家模型五輪對抗辯論定版的,每條規則都有出處。

先判**歌詞語言**(順序 **韓 → 日 → 中 → 英**;日文含漢字,必須排在中文前面判),讀對應那一份:

| 歌詞語言 | 讀這份 | 維度數 |
|---|---|---|
| 中文(國/粵/台/客) | `$T\rubrics\ZH_lyric_rubric_v5.md` | 7 |
| 英文 | `$T\rubrics\EN_lyric_rubric_v2.md` | 6 |
| 日文 | `$T\rubrics\JA_lyric_rubric_v3.md` | 6 |
| 韓文 | `$T\rubrics\KO_lyric_rubric_v4.md` | 7 |

⛔ **絕不把四把尺混在一起讀** —— 它們的軸互斥(韓文尺明文廢掉日文的音高重音與中文的倒字),混讀會把倒字規則套到韓文歌上。
⛔ **Craft(作品分)/ Reach(爆款分)禁止平均**;總分是整體判定(讀維度高低差),不是加權平均。
⭐ **歌詞語言 ≠ 報告語言**:常見情境是「日/韓文歌 + 中文報告」。維度名稱逐字取自 `rubrics\_維度對照表.md` 的「該尺 × 報告語言」列,不可自行翻譯。
其餘(開場協議、輸出模式、成績單格式、傳播檢查表、句級修法)一律照 `評詞標準.md`。

## 柱外項目(照列不計分)

🛠 **Prompt 工藝**(0-10,從抓到的 prompt 評標記完整度/演唱指示/文本衛生;只在有 prompt 時做)
+ **親聽檢查小節**(取自 `親聽檢查清單.md`)+ **顯示軸**(AI 感 SONICS、編曲細部、Audiobox CE/CU)
+ **❄ 凍結項**(可靠性驗證未過的量測,分數照列標 ❄ 不計入)。不入 PK 綜合分。
⛔ 唱詞正確性由人耳負責,**不用 ASR**(戲腔 Whisper 根本辨不出)。

## 報告落檔與清理

每次完整評測:對話貼成績單 + 所有產物收進 **`歌名_評分結果` 資料夾**:`歌名_歌詞.txt`、`歌名_評審團報告.md/.pdf`、`歌名_評審團報告_全長.png`、`歌名_歌詞_情感弧線.png`。報告第一行=歌名獨立大標。**對話、md、PDF 三者一字不差。**

報告產出並確認後,**刪除中間檔**:`_評審團.json`、`_情感弧線.json`。保留:歌檔、歌詞 txt、報告 md/PDF、弧線 png。

## 眉角(踩過的坑)

- **權重已定版**(重構庭 2026-07-25,十三席模型辯出),不再是暫定值;沿革見 `docs\權重沿革.md`。
  但分數仍是**診斷分**,不是客觀真理 —— 讀差距,別讀絕對值。
- **演唱分不進物理總分**:`song_scorer.py` 要加 `--blend-vocal` 才會併(預設不加)。演唱量測是
  透過九柱的人聲柱入分,不是靠 `scores.total`。
- **`分軌快取.py` / `伴奏混音.py` 是共用底層**,不是可有可無的附屬檔。新增 .py 進 repo 前,
  先確認有沒有別的檔 import 它 —— 白名單制 .gitignore 忘了放行就會整條線斷掉。
- **和聲分析冷跑 vs 讀快取分數會差最多 9 分**(flac 量化誤差翻轉和弦邊界)→ **PK 時所有參賽曲必須同時有快取或同時沒有**。
- **SongEval / Audiobox 有品味傾向**:實測給 AI 音樂的分高於得獎作品 → 只可同池相對比較,報告要標「品味註」。
- SUNO 連結評的是**未後製原始版**,與後製成品不可互比。
- SongEval 對戲腔/非主流唱法有偏見(人聲自然度會被壓低),讀數要在解讀欄註明打折。
- 歌詞 txt 給儀器前:段落標記統一用【】,戲腔的「——」拖腔符號要拆掉。
- 情感弧線儀的絕對值不可信(詞庫盲於反諷),只看段落間相對移動。
- **落款的唯一真理來源是 `報告轉PDF.py` 的 `CANON_FOOTER_1/2`**,轉檔時會強制覆蓋 md;要改必須兩邊同步。
- **批次評測預設不碰 Gemini**(`SONG_JURY_SKIP_GEMINI=1`):連發會把金鑰打爆,實測要等約 28 天才恢復。
