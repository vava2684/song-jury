# 評分工具鏈金鑰範本 —— 複製成 .env 再填值(.env 已被 .gitignore 擋住,不會進 repo)
#   cp .env.example .env

# Gemini 曲評(Gemini曲評.py)
# 多把金鑰:逗號分隔,程式會「依錯誤類別」輪替 —— 遇 429(額度/頻率)或金鑰無效/被拒才換下一把,
# 不是每次呼叫都換。換掉的那把會被記進 .gemini_key_state.json 冷卻,下次跑不會再白撞一次。
GEMINI_API_KEYS=你的第一把金鑰,你的第二把金鑰,你的第三把金鑰

# 只有一把時的相容寫法(上面那行可以整行刪掉)
# GEMINI_API_KEY=你的金鑰

# ⚠️ 免費額度建議每把用「不同的 Google Cloud 專案」開,同專案多金鑰共用同一份配額,輪替等於沒用。
